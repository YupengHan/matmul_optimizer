{
  "diagnosis_id": "diagnosis_20260418_220408",
  "status": "completed",
  "created_at": "2026-04-18T22:04:08-07:00",
  "source_run_id": "20260418_220017_bf16_gemm_v1_2e79574",
  "source_run_dir": "runs/20260418_220017_bf16_gemm_v1_2e79574",
  "source_summary_json": "runs/20260418_220017_bf16_gemm_v1_2e79574/summary.json",
  "source_ncu_summary_json": "state/latest_ncu_summary.json",
  "heuristics_path": "docs/heuristics.md",
  "current_kernel_path": "src/kernels/bf16_gemm_v1.cu",
  "recommended_direction_id": "dir_01",
  "approved_direction_id": null,
  "directions": [
    {
      "direction_id": "dir_01",
      "rank": 1,
      "name": "Widen the async staging path to 16-byte fixed-tile copies",
      "hypothesis": "The restored round-4 kernel keeps the good 2-warp, 2-output-fragment shape, but it still stages every A/B tile with 8-byte `cp.async` operations plus generic row and column address math. For this fixed shape, both source strides and tile origins are 16-byte aligned, so specializing the copy path to 16-byte moves should roughly halve the staging instruction count, reduce the dominant 39.12% `smsp__warp_issue_stalled_mio_throttle`, and leave more issue bandwidth for `wmma::load_matrix_sync` and MMA.",
      "expected_bottleneck": "Global-to-shared staging instruction pressure and MIO throttling from the current 8-byte async copy path, not a pure DRAM bandwidth ceiling.",
      "code_locations": [
        "src/kernels/bf16_gemm_v1.cu:30-39 (`kAsyncCopyElems` / `cp_async_copy_8_bytes`)",
        "src/kernels/bf16_gemm_v1.cu:58-69 (`stage_shared_tile_async` generic copy loop)",
        "src/kernels/bf16_gemm_v1.cu:150-169 (A/B warm-up and steady-state staging sites)"
      ],
      "risk": "Low to moderate. The shape and strides are favorable for 16-byte copies, but the implementation must preserve alignment for both A and B tiles and avoid reintroducing scalar fallback code that erases the instruction-count win.",
      "metrics_to_recheck": [
        "median runtime_ms",
        "smsp__warp_issue_stalled_mio_throttle_per_warp_active.pct",
        "smsp__warp_issue_stalled_long_scoreboard_per_warp_active.pct",
        "sm__pipe_tensor_cycles_active.avg.pct_of_peak_sustained_active",
        "gpu__compute_memory_throughput.avg.pct_of_peak_sustained_elapsed",
        "dram__throughput.avg.pct_of_peak_sustained_elapsed",
        "correctness cases"
      ],
      "stop_condition": "Stop if the 16-byte staging rewrite cannot stay fully aligned on both operands, or if `smsp__warp_issue_stalled_mio_throttle_per_warp_active.pct` and runtime both stay effectively flat after the copy count is reduced."
    },
    {
      "direction_id": "dir_02",
      "rank": 2,
      "name": "Skew the shared tiles for bank-friendlier WMMA fragment loads",
      "hypothesis": "The dominant exposed stall is still MIO pressure even after round 4 raised per-warp reuse, and the current `a_shared` and `b_shared` buffers are packed linearly with no skew or transpose before each warp loads one A fragment and two adjacent B fragments. Adding padding or a lightweight swizzle, especially on B, should reduce shared-bank serialization during `wmma::load_matrix_sync` and lift tensor-pipe activity without repeating the failed round-5 occupancy retune.",
      "expected_bottleneck": "Shared-memory and fragment-load pressure around `wmma::load_matrix_sync`, currently surfacing as high `smsp__warp_issue_stalled_mio_throttle` with only moderate DRAM and L2 throughput.",
      "code_locations": [
        "src/kernels/bf16_gemm_v1.cu:123-125 (shared A/B/C declarations and layout footprint)",
        "src/kernels/bf16_gemm_v1.cu:58-69 (how global tiles are packed into shared memory)",
        "src/kernels/bf16_gemm_v1.cu:172-179 (warp fragment base addresses and `wmma::load_matrix_sync`)"
      ],
      "risk": "Moderate. Shared-layout changes are easy to make subtly wrong, can interact badly with `cp.async` alignment assumptions, and may raise shared-memory footprint enough to give back some latency hiding.",
      "metrics_to_recheck": [
        "median runtime_ms",
        "smsp__warp_issue_stalled_mio_throttle_per_warp_active.pct",
        "sm__pipe_tensor_cycles_active.avg.pct_of_peak_sustained_active",
        "lts__throughput.avg.pct_of_peak_sustained_elapsed",
        "launch__shared_mem_per_block_allocated",
        "sm__warps_active.avg.pct_of_peak_sustained_active",
        "correctness cases"
      ],
      "stop_condition": "Stop if the new layout leaves `smsp__warp_issue_stalled_mio_throttle_per_warp_active.pct` stuck in the high-30% range, or if added padding/swizzle lowers active warps without a visible runtime gain."
    },
    {
      "direction_id": "dir_03",
      "rank": 3,
      "name": "Peel the fixed 7232-K loop into a branch-light steady-state pipeline",
      "hypothesis": "This benchmark has no tail tiles at all: `m`, `n`, and `k` are exact multiples of the current 32x32x16 tensor-core tile. The kernel still pays a per-iteration `next_tile_k < k` branch, stage-toggle bookkeeping, and a wait/sync cadence for all 452 K slices. Rewriting the pipeline as an explicit warm-up, fixed-trip steady state, and drain path should cut control overhead, make the barrier schedule easier for the compiler to optimize, and improve tensor-core utilization without revisiting the round-5 warp-split experiment.",
      "expected_bottleneck": "Synchronization and control overhead inside the generic per-slice K loop, showing up as persistent barrier and long-scoreboard stalls even after the round-4 reuse win.",
      "code_locations": [
        "src/kernels/bf16_gemm_v1.cu:147-154 (pipeline warm-up)",
        "src/kernels/bf16_gemm_v1.cu:156-185 (steady-state K loop, stage toggling, wait/sync)",
        "src/kernels/bf16_gemm_v1.cu:239-246 (shape gating and tensor-core launch path)"
      ],
      "risk": "Moderate to high. The fixed-shape rewrite is structurally bigger than the first two options, and a sloppy warm-up or drain rewrite can quietly break correctness or increase register pressure past the current 40 registers per thread.",
      "metrics_to_recheck": [
        "median runtime_ms",
        "smsp__warp_issue_stalled_barrier_per_warp_active.pct",
        "smsp__warp_issue_stalled_long_scoreboard_per_warp_active.pct",
        "sm__pipe_tensor_cycles_active.avg.pct_of_peak_sustained_active",
        "launch__registers_per_thread",
        "sm__warps_active.avg.pct_of_peak_sustained_active",
        "correctness cases"
      ],
      "stop_condition": "Stop if the fixed-shape rewrite only reshuffles waits without lowering barrier or long-scoreboard stalls, or if register pressure climbs enough to push active warps materially below the current baseline."
    }
  ],
  "notes": "Run node_b to produce exactly three ranked directions."
}
