{
  "diagnosis_id": "diagnosis_20260418_222704",
  "status": "completed",
  "created_at": "2026-04-18T22:27:04-07:00",
  "source_run_id": "20260418_222639_bf16_gemm_v1_95056ed",
  "source_run_dir": "runs/20260418_222639_bf16_gemm_v1_95056ed",
  "source_summary_json": "runs/20260418_222639_bf16_gemm_v1_95056ed/summary.json",
  "source_ncu_summary_json": "state/latest_ncu_summary.json",
  "heuristics_path": "docs/heuristics.md",
  "current_kernel_path": "src/kernels/bf16_gemm_v1.cu",
  "recommended_direction_id": "dir_01",
  "approved_direction_id": "dir_01",
  "directions": [
    {
      "direction_id": "dir_01",
      "rank": 1,
      "name": "Skew the B shared tile with a bank-conflict-avoidance swizzle",
      "idea_origin": "human-idea",
      "hypothesis": "The accepted 16x48 BF16 B tile is still loaded through `wmma::load_matrix_sync` with a 48-element shared stride, so each row advances by 96 bytes and repeatedly aliases the same shared-memory banks during the three adjacent matrix_b fragment loads. Adding a per-row skew to the staged B tile, while preserving 16-byte async-copy granularity, should cut the remaining `mio_throttle` and short-scoreboard pressure without undoing the 3xN reuse win from round 2.",
      "expected_bottleneck": "Residual bank conflicts and port contention on the shared-memory-to-WMMA B-fragment feed path, which still show up as `smsp__warp_issue_stalled_mio_throttle_per_warp_active.pct = 25.61` and `smsp__warp_issue_stalled_short_scoreboard_per_warp_active.pct = 11.28` even after the tile-retune improvement.",
      "code_locations": [
        "src/kernels/bf16_gemm_v1.cu:22-36 (`kTensorBlockN`, `kBSharedTileElems`, async-copy sizing constants)",
        "src/kernels/bf16_gemm_v1.cu:82-93 (`stage_b_shared_tile_async` shared-store layout)",
        "src/kernels/bf16_gemm_v1.cu:147 (`b_shared` declaration)",
        "src/kernels/bf16_gemm_v1.cu:196-201 (`wmma::load_matrix_sync` B-fragment loads and leading dimension)"
      ],
      "risk": "Moderate. The skew must preserve both `cp.async` alignment and the logical row-major matrix view expected by `wmma::load_matrix_sync`; a bad stride choice can silently corrupt results or increase shared-memory footprint enough to hurt occupancy.",
      "metrics_to_recheck": [
        "median runtime",
        "TFLOP/s",
        "smsp__warp_issue_stalled_mio_throttle_per_warp_active.pct",
        "smsp__warp_issue_stalled_short_scoreboard_per_warp_active.pct",
        "sm__pipe_tensor_cycles_active.avg.pct_of_peak_sustained_active",
        "sm__warps_active.avg.pct_of_peak_sustained_active",
        "launch__shared_mem_per_block_allocated",
        "correctness cases"
      ],
      "stop_condition": "Stop if the skewed layout fails correctness, or if `mio_throttle` stays in the same mid-20% range without a runtime win versus 66.354687 ms."
    },
    {
      "direction_id": "dir_02",
      "rank": 2,
      "name": "Split the B tile into three independently padded 16x16 shared subtiles",
      "idea_origin": "human-idea",
      "hypothesis": "If the 48-column aggregate B tile is the real source of bank aliasing, staging each 16x16 MMA subtile separately with its own padded stride should remove the wide-ld interaction without changing the math schedule. This is a more explicit variant of the bank-conflict fix that trades a slightly larger address-calculation surface for a cleaner WMMA load shape.",
      "expected_bottleneck": "The combined 16x48 row-major B layout may be making the three adjacent fragment loads interfere with each other at shared-memory bank granularity even though the per-warp compute reuse is otherwise correct.",
      "code_locations": [
        "src/kernels/bf16_gemm_v1.cu:24-36 (tile-shape constants for the B stage)",
        "src/kernels/bf16_gemm_v1.cu:82-93 (`stage_b_shared_tile_async` loop structure)",
        "src/kernels/bf16_gemm_v1.cu:147 (`b_shared` declaration)",
        "src/kernels/bf16_gemm_v1.cu:196-201 (B tile base pointers per `tile_n`)"
      ],
      "risk": "Moderate to high. Splitting the tile complicates copy scheduling and can expand the shared footprint or address arithmetic enough to offset the bank-conflict win.",
      "metrics_to_recheck": [
        "median runtime",
        "TFLOP/s",
        "smsp__warp_issue_stalled_mio_throttle_per_warp_active.pct",
        "smsp__warp_issue_stalled_short_scoreboard_per_warp_active.pct",
        "sm__throughput.avg.pct_of_peak_sustained_elapsed",
        "launch__shared_mem_per_block_allocated",
        "launch__registers_per_thread"
      ],
      "stop_condition": "Stop if the per-subtile layout makes the code materially more complex without reducing the shared-memory stall metrics or runtime."
    },
    {
      "direction_id": "dir_03",
      "rank": 3,
      "name": "Transpose B into a conflict-friendlier shared layout and switch the WMMA load orientation",
      "idea_origin": "human-idea",
      "hypothesis": "A transpose into a col-major-compatible shared tile could eliminate the current row-major bank pattern entirely and let the warp issue B fragment loads from a layout that better matches tensor-core ingestion. This is the most invasive shared-layout fix but would directly test whether the current row-major staging choice is the real MIO limiter.",
      "expected_bottleneck": "The row-major B shared layout may be fundamentally mismatched to the warp-level matrix_b load pattern, keeping the tensor pipe underfed even after the earlier global-to-shared improvements.",
      "code_locations": [
        "src/kernels/bf16_gemm_v1.cu:82-93 (`stage_b_shared_tile_async`)",
        "src/kernels/bf16_gemm_v1.cu:147 (`b_shared` declaration)",
        "src/kernels/bf16_gemm_v1.cu:184-201 (matrix_b fragment type and load orientation)"
      ],
      "risk": "High. A layout-orientation change touches both staging and WMMA semantics and is easier to get wrong than a stride skew.",
      "metrics_to_recheck": [
        "median runtime",
        "TFLOP/s",
        "smsp__warp_issue_stalled_mio_throttle_per_warp_active.pct",
        "sm__pipe_tensor_cycles_active.avg.pct_of_peak_sustained_active",
        "sm__warps_active.avg.pct_of_peak_sustained_active",
        "correctness cases"
      ],
      "stop_condition": "Stop if the transposed/shared-orientation variant threatens correctness or requires broad kernel surgery before showing a measurable stall reduction."
    }
  ],
  "notes": "Human-directed round 3: focus on shared-memory bank conflicts and a swizzle/skew fix for the B WMMA feed path."
}
