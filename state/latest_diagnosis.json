{
  "diagnosis_id": "diagnosis_20260419_190126",
  "status": "completed",
  "created_at": "2026-04-19T19:01:26-07:00",
  "source_run_id": "20260419_185736_bf16_gemm_v1_85bb65b",
  "source_run_dir": "runs/20260419_185736_bf16_gemm_v1_85bb65b",
  "source_summary_json": "runs/20260419_185736_bf16_gemm_v1_85bb65b/summary.json",
  "source_ncu_summary_json": "state/latest_ncu_summary.json",
  "heuristics_path": "docs/heuristics.md",
  "current_kernel_path": "src/kernels/bf16_gemm_v1.cu",
  "recommended_direction_id": "dir_01",
  "approved_direction_id": null,
  "directions": [
    {
      "direction_id": "dir_01",
      "rank": 1,
      "name": "Human idea 1 Tiling: keep the 256x128/64x64 family, but add paired 64x64 export scratch to cut warp barrier tax",
      "hypothesis": "The new tiling family is the real mainline now. Its first cut beat the previous accepted base by 2.4530878 ms and the hot 256x128 kernel changed the machine state in exactly the way a good structural pivot should: long scoreboard is down at 0.55, DRAM throughput is only 17.85, and tensor active is up to 37.63. That means the new family already solved the old memory-feed problem. The remaining taxes are local to the new kernel itself: barrier is still 20.76, short-scoreboard is 6.78, and the export path still serializes each 16x16 sub-tile through a single per-warp scratch tile with repeated __syncwarp. The most concrete next move is therefore to stay inside Human idea 1 and spend some of the 48 KB shared-memory budget headroom on a two-scratch export path for the 64x64 warp tile so the new family can reduce warp-local export/barrier overhead without changing its macro tiling.",
      "expected_bottleneck": "Warp-local export and synchronization overhead in the 256x128/64x64 kernel, now visible as high barrier stall and elevated short-scoreboard after memory starvation has already been fixed.",
      "code_locations": [
        "src/kernels/bf16_gemm_v1.cu:80-105",
        "src/kernels/bf16_gemm_v1.cu:664-709",
        "src/kernels/bf16_gemm_v1.cu:1212-1297"
      ],
      "risk": "This raises shared-memory usage in the pivot kernel. If the extra scratch is implemented clumsily, it could increase register pressure or fail to reduce sync density enough to matter. The change must stay local to the new 256x128 branch and not spill over into the stable 64x384 residual path.",
      "metrics_to_recheck": [
        "median runtime",
        "smsp__warp_issue_stalled_barrier_per_warp_active.pct",
        "smsp__warp_issue_stalled_short_scoreboard_per_warp_active.pct",
        "sm__pipe_tensor_cycles_active.avg.pct_of_peak_sustained_active",
        "launch__shared_mem_per_block_allocated",
        "launch__registers_per_thread"
      ],
      "stop_condition": "Stop if paired export scratch does not reduce barrier or short-scoreboard, or if the added shared-memory footprint harms runtime without a clear tensor-active gain. Then export tax is not the main limiter inside this family."
    },
    {
      "direction_id": "dir_02",
      "rank": 2,
      "name": "Human idea 7 Register reuse: trim 64x64 fragment live state to chase the occupancy and short-scoreboard tax",
      "hypothesis": "The hot 256x128 kernel is still register-limited at 166 regs/thread and occupancy_limit_registers remains 1. Its 64x64 warp kernel currently loads full arrays of four A fragments and four B fragments before issuing the 4x4 mma tile set, which is a plausible source of both register pressure and short-scoreboard delay. A family-consistent follow-up is to keep the new 256x128 tiling but change the 64x64 warp-local accumulate helper so it streams a smaller fragment working set across the same 4x4 tile, reducing fragment residency rather than changing the macro tile.",
      "expected_bottleneck": "Register and warp-local fragment live-state pressure inside the 64x64 PTX micro-tile, limiting occupancy and contributing to short-scoreboard.",
      "code_locations": [
        "src/kernels/bf16_gemm_v1.cu:503-567",
        "src/kernels/bf16_gemm_v1.cu:1203-1297",
        "src/kernels/bf16_gemm_v1.cu:1347-1354"
      ],
      "risk": "This can easily trade one bottleneck for another. If the streamed fragment plan reloads too aggressively, it may reintroduce long scoreboard or lower tensor utilization. It is also a more invasive code-motion change than the export-scratch follow-up.",
      "metrics_to_recheck": [
        "launch__registers_per_thread",
        "launch__occupancy_limit_registers",
        "smsp__warp_issue_stalled_short_scoreboard_per_warp_active.pct",
        "sm__warps_active.avg.pct_of_peak_sustained_active",
        "sm__pipe_tensor_cycles_active.avg.pct_of_peak_sustained_active",
        "median runtime"
      ],
      "stop_condition": "Stop if registers do not fall or if occupancy and short-scoreboard stay flat while runtime regresses. Then fragment live state is not the right next tax to attack."
    },
    {
      "direction_id": "dir_03",
      "rank": 3,
      "name": "Human idea 5 Bank conflict: retune the 64x64 branch's shared layout and warp load pattern only",
      "hypothesis": "With DRAM low and long scoreboard nearly gone, the remaining short-scoreboard component may partly come from the new family's shared-memory access pattern rather than from global latency. The 256x128 branch introduced a materially different 128-wide B tile and 64x64 warp micro-tile, so old negative evidence against broad B-feed rewrites does not fully rule out a very local bank/layout retune here. A narrow experiment that changes only the 64x64 branch's shared stride or warp-fragment load order could relieve shared-bank pressure without reopening the old CTA-wide B-repack families.",
      "expected_bottleneck": "Shared-memory bank/load behavior inside the new 64x64 warp tile, contributing to short-scoreboard and LSU wavefront pressure.",
      "code_locations": [
        "src/kernels/bf16_gemm_v1.cu:80-105",
        "src/kernels/bf16_gemm_v1.cu:521-530",
        "src/kernels/bf16_gemm_v1.cu:731-742",
        "src/kernels/bf16_gemm_v1.cu:1261-1269"
      ],
      "risk": "This is weaker evidence than the barrier/export and register paths. It can also accidentally recreate the kind of B-feed experiments that already failed on the older families if it grows beyond a strictly local 64x64 branch change.",
      "metrics_to_recheck": [
        "smsp__warp_issue_stalled_short_scoreboard_per_warp_active.pct",
        "l1tex__data_pipe_lsu_wavefronts.avg.pct_of_peak_sustained_elapsed",
        "l1tex__data_bank_reads.avg.pct_of_peak_sustained_elapsed",
        "l1tex__data_bank_writes.avg.pct_of_peak_sustained_elapsed",
        "sm__pipe_tensor_cycles_active.avg.pct_of_peak_sustained_active",
        "median runtime"
      ],
      "stop_condition": "Stop if bank and LSU metrics stay flat or if runtime does not improve. Then the short-scoreboard tax is not mainly a shared-layout problem."
    }
  ],
  "notes": "Round 14 is a continue-family move for Human idea 1 / Tiling. The measured reason is strong: the first-cut 256x128 CTA / 64x64 warp pivot improved runtime by 2.4530878 ms and established a new accepted base at 30.9743681 ms. More importantly, the raw hot-kernel machine state says the family is solving the right problem. For the new bf16_gemm_v1_tensor_core_fixed_hot_band_256x128_kernel, launch__registers_per_thread is 166, shared_mem_per_block_allocated is 34.304 KB, sm__pipe_tensor_cycles_active is 37.63, barrier stall is 20.76, long scoreboard is only 0.55, short scoreboard is 6.78, mio is 2.10, active warps is 16.63, and dram throughput is only 17.85. That is exactly the signature of a family worth continuing: memory scoreboard is already mostly eliminated, so the next wins should come from reducing barrier/export tax and then register/live-state tax inside the new family rather than pivoting away immediately.\n\nHuman idea audit for this round:\n1. Tiling (256x128 block, 64x64 warp): accept-now and rank first. The new tiling family just produced the largest win in many rounds and clearly deserves continued multi-round exploration.\n2. Coalescing Access: defer. The branch already uses wide 16-byte copies, and DRAM throughput is low, so global coalescing is not the next primary limiter.\n3. Data Reuse: accept-now as a secondary lever inside the tiling family. The recommended direction uses more effective per-warp export scratch reuse rather than a new macro-tile rewrite.\n4. Async Copy: accept-now only as already-working baseline behavior. Long scoreboard and DRAM throughput are low, so async copy is not the first tax to attack next.\n5. Bank Conflict: accept-now as a lower-ranked local follow-up. The new 64x64 branch has a different shared-access pattern, so a narrow bank/layout tune is newly plausible even though older broad B-feed rewrites failed.\n6. L2 Cache: reject-for-this-round. The current hot kernel is not L2 or DRAM bound.\n7. Register Reuse: accept-now and rank second. Register/occupancy tax inside the new 64x64 warp tile is a real next bottleneck, but the export/barrier path is a more direct first target.\n8. Pg2s: accept-now only as already-adopted baseline behavior. Global-to-shared prefetch is not the current gating issue.\n9. Ps2r: defer. Long scoreboard is already down at 0.55 in the new family, so a Ps2r-first follow-up is not the highest-value next move.\n10. Stage: reject-for-this-round as a primary family. The new tiling branch already solved the old feed starvation more effectively than the demoted Stage family did, so there is no reason to reopen it now."
}
