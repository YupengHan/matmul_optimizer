{
  "diagnosis_id": "diagnosis_20260418_200741",
  "status": "completed",
  "created_at": "2026-04-18T20:07:41-07:00",
  "source_run_id": "20260418_195405_bf16_gemm_v1_a4966f5",
  "source_run_dir": "runs/20260418_195405_bf16_gemm_v1_a4966f5",
  "source_summary_json": "runs/20260418_195405_bf16_gemm_v1_a4966f5/summary.json",
  "source_ncu_summary_json": "state/latest_ncu_summary.json",
  "heuristics_path": "docs/heuristics.md",
  "current_kernel_path": "src/kernels/bf16_gemm_v1.cu",
  "recommended_direction_id": "dir_01",
  "approved_direction_id": null,
  "directions": [
    {
      "direction_id": "dir_01",
      "rank": 1,
      "name": "Add CTA-level shared-memory staging for WMMA tiles",
      "hypothesis": "The new WMMA kernel moved math onto Tensor Cores, but each warp still loads its A and B fragments directly from global memory with no cross-warp reuse. The profile now looks memory-led: tensor pipe active is only 4.78%, DRAM/L2 throughput is much higher than SM throughput, and long scoreboard plus MIO throttle stalls dominate. A shared-memory staging path with CTA-level reuse, ideally double-buffered, is the highest-upside next step because it should turn repeated global fragment fetches into reused BF16 tiles that feed multiple warps.",
      "expected_bottleneck": "Global-memory bound",
      "code_locations": [
        "src/kernels/bf16_gemm_v1.cu::bf16_gemm_v1_tensor_core_kernel",
        "src/kernels/bf16_gemm_v1.cu::launch_bf16_gemm_v1"
      ],
      "risk": "Moderate to high: shared-memory layout, load choreography, and WMMA-compatible tile packing all become more complex, and a bad layout can trade one memory bottleneck for another.",
      "metrics_to_recheck": [
        "sm__pipe_tensor_cycles_active.avg.pct_of_peak_sustained_active",
        "smsp__warp_issue_stalled_long_scoreboard_per_warp_active.pct",
        "smsp__warp_issue_stalled_mio_throttle_per_warp_active.pct",
        "dram__throughput.avg.pct_of_peak_sustained_elapsed",
        "median_runtime_ms"
      ],
      "stop_condition": "Stop if staged tiles do not materially lower scoreboard or MIO stalls and tensor activity stays near the same level, because then the added shared-memory pipeline is not improving feed rate to the Tensor Cores."
    },
    {
      "direction_id": "dir_02",
      "rank": 2,
      "name": "Retune the WMMA tile hierarchy for more per-warp accumulation",
      "hypothesis": "The current Tensor Core kernel assigns one 16x16x16 MMA tile per warp at a time and only forms a 32x32 CTA tile, which leaves arithmetic intensity low and duplicates A/B traffic across neighboring warps. Expanding the warp or CTA tile shape so each warp accumulates multiple MMA tiles before writing out should improve data reuse and keep Tensor Cores busy for longer per byte loaded.",
      "expected_bottleneck": "Tensor Core under-utilization",
      "code_locations": [
        "src/kernels/bf16_gemm_v1.cu::bf16_gemm_v1_tensor_core_kernel",
        "src/kernels/bf16_gemm_v1.cu"
      ],
      "risk": "Moderate: larger fragment sets and wider tiles can increase register pressure or hurt occupancy, so the benefit depends on finding a shape that raises reuse without overfilling the SM.",
      "metrics_to_recheck": [
        "sm__pipe_tensor_cycles_active.avg.pct_of_peak_sustained_active",
        "sm__warps_active.avg.pct_of_peak_sustained_active",
        "launch__occupancy_limit_registers",
        "tflops",
        "median_runtime_ms"
      ],
      "stop_condition": "Stop if a larger tile shape drives up register pressure or drops active warps without a clear TFLOP/s gain, because then the hierarchy got too large for this GPU."
    },
    {
      "direction_id": "dir_03",
      "rank": 3,
      "name": "Specialize the hot path to the fixed aligned benchmark shape",
      "hypothesis": "This benchmark shape is fully 16-aligned in M, N, and K, but the launch path still carries a generic runtime divisibility check and a general-purpose fallback kernel. Hardwiring the fixed-shape steady state, launch geometry, and K-loop trip count for the benchmark can simplify control flow and make subsequent staged WMMA kernels easier to optimize and profile.",
      "expected_bottleneck": "Tail-handling overhead from generic code",
      "code_locations": [
        "src/kernels/bf16_gemm_v1.cu::launch_bf16_gemm_v1",
        "src/kernels/bf16_gemm_v1.cu::bf16_gemm_v1_tensor_core_kernel"
      ],
      "risk": "Low to moderate: the code change is contained, but the standalone speedup may be limited because the dominant loss today is still memory feed into the Tensor Core path.",
      "metrics_to_recheck": [
        "median_runtime_ms",
        "tflops",
        "sm__throughput.avg.pct_of_peak_sustained_elapsed",
        "sm__pipe_tensor_cycles_active.avg.pct_of_peak_sustained_active"
      ],
      "stop_condition": "Stop if fixed-shape specialization only produces noise-level runtime movement, because then the generic launch path is not a material contributor at this stage."
    }
  ],
  "notes": "Run node_b to produce exactly three ranked directions."
}
